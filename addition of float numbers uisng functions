#include <stdio.h>

int add(float a, float b)
{
    float sum;
    sum=a+b;
    return (sum);
}

int main()
{
  float a,b,c;
  printf("Enter the numbers to add\n");
  scanf("%f%f",&a,&b);
  c=add(a,b);
  printf("sum is %f",c);
  return 0;
}
