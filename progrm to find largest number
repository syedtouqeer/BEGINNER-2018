#include<stdio.h>
#include<math.h>

int main()
{
   int x, y;
   printf("Enter two numbers:\n");
   scanf("%d %d", &x, &y);
   printf("\nLargest number: %d\n", ((x + y) + abs(x - y)) / 2);

   return 0;
}
